using UnityEngine;

namespace NodeZero.Inventory
{
    // Marker component retained by the slot prefab. Grid occupancy is managed by InventoryGrid.
    public sealed class InventorySlotUI : MonoBehaviour
    {
    }
}
